[workspace]
channels = ["conda-forge", "bioconda"]
name = "tests"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
singularity = ">=3.8.6,<4"
apptainer = ">=1.1.3,<2"
docker-cli = ">=29.4.0,<30"
